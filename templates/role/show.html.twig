{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1 class="my-4 text-center">Role</h1>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ role.rang }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ role.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-md bg_affich m-2" href="{{ path('app_role_index') }}"><ion-icon name="return-down-back-outline"></ion-icon><br>back to list</a>

    <a class="btn btn-md bg_modif m-2" href="{{ path('app_role_edit', {'id': role.id}) }}"><ion-icon name="pencil-outline"></ion-icon><br>edit</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
