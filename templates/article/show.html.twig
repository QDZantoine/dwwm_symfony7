{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
<div class="m-auto w50 py-4">
    <h1 class="titre center text-light" >Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>NumArticle</th>
                <td>{{ article.numArticle }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ article.designation }}</td>
            </tr>
            <tr>
                <th>PrixUnitaire</th>
                <td>{{ article.prixUnitaire }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-md bg_affich"  href="{{ path('app_article_index') }}">Retour à la liste</a>

    <a class="btn btn-md  bg_modif  m-2" href="{{ path('app_article_edit', {'id': article.id}) }}">Modifier</a>

    {{ include('article/_delete_form.html.twig') }}
</div>
{% endblock %}
